"use client";

import { ClaimApprovalView } from "@/features/claims/components/ClaimApprovalView";

/** Top Management — org-wide claims (same table UX as personal All Claims + Submitted by). */
export default function AllClaimsPage() {
  return <ClaimApprovalView />;
}
